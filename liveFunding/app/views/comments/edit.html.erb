<h1>Edit your comment:</h1>
<% form_for([@transaction, @comment]) do |f| %>
	
	<%= f.label :content %><br />
	<%= f.text_area :content %>
	
	<p>
		<%= f.submit "Edit" %>
	</p>
<% end %>
<%= link_to 'Back', transaction_path(@transaction) %>